# 云五笔协作总纲（PLAN — 豆包 ⇄ TRAE 共同执行）

## 一、目标（Goal）
**总目标**：做一款"能用、好用、越用越顺"的五笔输入法——编码真实正确、词组优先、候选上屏流畅、体积精简、代码干净、云端词库持续增强，用户可盲打、效率高。

**当前阶段目标**：
- 阶段 1（进行中）：功能正确——T1 接入真实词库 + 候选点击上屏 + 空格上屏（"打得对"）
- 阶段 2：UI 按国际标准——先布局（房子）后风格（装修）再功能配套
- 阶段 3：词库扩充与云端优化、体积控制、性能提速

## 二、任务（Task）— 全局编号，递增执行
| 编号 | 任务 | 状态 |
|---|---|---|
| T1 | 接入真实词库（wubi86 单字+词组）+ 候选点击上屏 + 空格上屏 | 进行中（inbox 已派发）|
| T2 | UI 布局按国际标准（键盘分区/键位/功能区），先出布局图验收 | 待派发 |
| T3 | 云端词库下发速度/缓存优化（词组快速出） | 待派发 |
| T4+ | 词库扩充（高频/成语/日常用语）、体积优化、语音等增强 | 按序排队 |

## 三、计划（Plan）
按 T1 → T2 → T3 → T4… 顺序推进；每项必须通过验收标准才算完成，再进入下一项；不跳步、不返工。

## 四、顺序（Order）
功能正确（编码/词库/上屏）→ UI 布局（房子）→ UI 风格（装修）→ 功能增强（语音等配套）→ 优化（体积/速度/云端链路）。

## 五、流程（Process）
豆包写任务(inbox) → TRAE pull 领取 → TRAE 执行(改码/编译/装夜神/测试/截图) → TRAE 回报(outbox, FSABE 结构) → 豆包核验(对照验收标准) → 通过=派下一项 / 不通过=写修复点回 inbox → 阶段报告(对话+Memos)。

## 六、要求（Requirement）
1. **FSABE 五步法则**：每轮任务与回报必须按 F(纯事实,不掺假设) → A(分析,做/不做/做好/做坏的后果) → B(二元结论:好/坏、快/慢、盈/亏 + 对用户/产品影响) → E(方案:2-3 定向选项,末项"或其他") 执行
2. **验收不过不标记完成**：每项任务有明确验收标准（见 inbox）
3. **同一问题不允许二次出现**：修复过的问题必须回归验证，禁止复发
4. **协作氛围**：全过程互相欣赏、互相鼓励、互相支持，直到成功
5. **用户把控**：用户提出要求/意见/建议，最终验收

## 七、标准（Standard）
| 项 | 标准 |
|---|---|
| 五笔规范 | 86 版王码，真实编码表（Rime 来源，勿用臆造编码）|
| 显示顺序 | 高频字 → 二字词 → 三字词 → 四字词 → 联想词（仅补全可选，不前置）|
| 简体 | 严禁繁体 |
| 体积 | 安装包 ≤100KB 目标 |
| 代码 | 干净无残留、无垃圾代码、无重复问题 |
| 测试 | 发布前 ≥50 项检查全绿 + 真机/模拟器实测 |
| 云端 | 三字及以上词放云端，控客户端体积 |

## 八、三方共识确认
- 豆包：主导（算法/词库/验收/派活/核验）
- TRAE：配合（编译/真机/截图/执行/回报）
- 用户：把控方向、提要求、最终验收
- 沟通：桥梁(cloudwubi-bridge) + 阶段报告(对话 + zsnote Memos 私密日志)
